---
interface Props {
  title: string
  backTo?: {
    href: string
    label: string
  }
}

const { title, backTo } = Astro.props
---

<div class="relative w-full px-2 pt-2 sm:px-10 sm:pt-8">
  {
    backTo && (
      <nav class="absolute top-2 left-2 z-10 sm:top-10 sm:left-10">
        <a
          href={backTo.href}
          class="font-eina04 group inline-flex items-center gap-2 font-medium text-gray-600 transition-colors duration-200 hover:text-gray-900"
          aria-label={`Return to ${backTo.label}`}
        >
          <svg
            class="h-4 w-4 transition-transform duration-200 group-hover:-translate-x-1"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M15 19l-7-7 7-7"
            />
          </svg>
          <span class="text-sm sm:text-base">{backTo.label}</span>
        </a>
      </nav>
    )
  }

  <h1
    class="font-eina01 w-full pt-4 pb-8 text-6xl font-semibold text-gray-900 sm:pt-16 sm:pb-36 sm:text-8xl"
  >
    {title}
  </h1>
</div>
