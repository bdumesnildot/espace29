---
import Button from "@components/ui/button/Button.astro"
import Layout from "@layouts/Layout.astro"
---

<Layout title="e29 | Page non trouvée">
  <main class="flex h-[calc(100vh-60px)] w-full flex-col items-start">
    <section
      class="flex w-full grow flex-col items-center justify-center px-2 py-4 sm:px-10 sm:py-10"
    >
      <div class="mx-auto max-w-4xl text-center">
        <!-- Error Code -->
        <div class="mb-6">
          <h1 class="font-eina01 mb-3 text-5xl font-semibold text-gray-900">
            404
          </h1>
          <div class="mx-auto mb-6 h-px w-16 bg-gray-300"></div>
        </div>

        <!-- Error Message -->
        <div class="mb-8">
          <h2 class="font-eina02 mb-4 text-3xl font-semibold text-gray-900">
            Page non trouvée
          </h2>
          <p class="font-eina03 mb-4 text-lg leading-relaxed text-gray-700">
            Désolé, la page que vous cherchez n'existe pas ou a été déplacée.
          </p>
          <p class="font-eina03 text-lg text-gray-700">
            Vérifiez l'URL ou retournez à l'accueil pour découvrir notre
            programmation artistique.
          </p>
        </div>

        <!-- Actions -->
        <div
          class="flex flex-col items-center justify-center gap-4 sm:flex-row"
        >
          <a href="/" class="inline-block">
            <Button intent="primary" size="small"> Retour à l'accueil </Button>
          </a>
          <a href="/agenda" class="inline-block">
            <Button intent="secondary" size="small"> Voir l'agenda </Button>
          </a>
        </div>
      </div>
    </section>
  </main>
</Layout>
